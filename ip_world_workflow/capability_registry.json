{
  "registryVersion": 1,
  "updatedAt": "2026-07-03",
  "purpose": "Single source of truth for external capabilities every world run must use. init-run snapshots this file into the run dir as capabilities.json. Gates cross-check decisions against these entries so runs cannot quietly fall back to ad hoc tools.",
  "englishExpressionSpace": {
    "spaceId": "9cb2a38e-2a84-4b1a-bcf4-cb4dff1cd0b4",
    "spaceName": "en·英文表达助手",
    "use": "All translation or polishing of visible copy. Default is direct English authoring; any translation pass must leave a cache file whose meta.spaceId matches this entry.",
    "forbiddenAlternatives": [
      "translate.googleapis.com",
      "browser translation",
      "generic web translation APIs"
    ]
  },
  "styleSpace": {
    "spaceId": "d95744b4-07f6-4836-8209-f1c6ece7658b",
    "spaceName": "Studio_ArtStyle",
    "use": "World-level style selection. style_decision.json selectedStyle must come from style_space or approved_style_library.",
    "verifyAtRunStart": "cohub spaces ls | grep -i artstyle"
  },
  "fandomCli": {
    "command": "fandom",
    "use": "Source discovery, category-tree traversal and reference-image recovery. Not optional. Cloudflare blocks direct api.php from Cohub sandboxes; from a sandbox use r.jina.ai transport instead.",
    "sandboxTransport": "r.jina.ai wrapping fandom api.php"
  },
  "importSamples": {
    "use": "Known-good narrating-import package shapes. Consult before writing any new import builder; never guess the schema.",
    "paths": [
      "/Users/atou/agents-in-discord/workspaces/1522485063688650762/deliverables/chainsaw-man/import/world_import.json"
    ],
    "shapeNotes": "narrating import consumes sections[].atoms, not a root-level atom table. Atom ids are generated by the importer."
  },
  "studioAuth": {
    "use": "Studio world create and provision need owner auth. Local shared Chrome owner session is the default; a sandbox needs an exported STUDIO_COOKIE plus browser-like user agent.",
    "localDefault": "shared Chrome owner session via CDP",
    "sandboxBootstrap": "temporary STUDIO_COOKIE file"
  },
  "externalTaskPolicy": {
    "use": "Any batch task that calls external endpoints (reference-image recovery, translation batches, cover generation polling).",
    "rules": [
      "per-item timeout with skip-and-record, never let one slow item block the batch",
      "run long batches in the background, poll state instead of foreground-waiting",
      "record skipped items in a repair list and close them before the stage gate, or document them as known gaps"
    ]
  }
}

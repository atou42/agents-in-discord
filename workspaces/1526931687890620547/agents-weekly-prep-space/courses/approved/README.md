# Approved Courses

Only create a course directory here after structural validation passes and a clean-context acceptance reviewer returns `READY`. Each approved directory contains exactly `course-package.json`, `acceptance.md`, and `recall-questions.md`. Raw sources, reviews, prompts, drafts, and internal status stay in `courses/active/`.

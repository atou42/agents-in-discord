# 本机任务提交验收

日期：2026-09-19。工作区：`/Users/atou/agents-in-discord-task-submit`。

`VERDICT: PARTIAL`

本地实现及相关回归通过；真实 Discord/Codex 验收未执行，因为尚未授权整合运行目录、启用配置和重启实例。此处不以可控替身测试声称端到端成功。

## 最终命令与结果

```bash
node --test test/local-task-submission.test.mjs test/channel-queue.test.mjs test/channel-queue-retry.test.mjs test/channel-queue-steer-delivery.test.mjs test/channel-runtime.test.mjs test/prompt-orchestrator.test.mjs test/prompt-orchestrator-native-images.test.mjs test/prompt-runtime.test.mjs test/session-store.test.mjs test/session-store-integrity.test.mjs test/discord-entry-handlers.test.mjs test/app-context.test.mjs
```

关键输出：

```text
tests 174
pass 174
fail 0
cancelled 0
skipped 0
duration_ms 7462.894458
```

其中新增入口测试 23 项，相关已有回归 151 项。测试中的 `boom`、`disk write failure` 等错误日志来自刻意注入的失败路径，断言及退出码均通过。

```bash
node --check src/index.js
git diff --check
```

两项均退出码 0，无输出。未启动 `src/index.js`，未执行生产启动/重启脚本。

## 已证明的行为

| 检查 | 实际执行与断言 | 结果 |
| --- | --- | --- |
| 提交与进度状态 | 用真实 session-store、channel-queue、prompt-orchestrator 和文件锁运行任务；断言 accepted 到 started、succeeded 的历史以及 runner 调用时已持锁 | PASS |
| 独立会话与工作区 | 首次 runner 输入 session ID 为 null，未复制来源模型，路径为独立 agent-tasks/thread-id | PASS |
| 后续交互 | 通过真实 Discord entry handler 送入普通用户后续消息，第二轮取得同一 session ID；bot 消息仍不执行 | PASS |
| 授权 | 真实入口确认用户授权命令且不入队；bot、自然语言、未开放用户/目标、缺少 Discord 权限均拒绝 | PASS |
| 幂等与冲突 | 连续重复提交及实际 CLI 重提不新增 thread/任务；同 ID 不同 prompt 明确冲突 | PASS |
| 长 prompt | 多字节长文本首条消息附件与原 prompt 完全一致，forum 路径也通过；正文仍完整进入 runner | PASS |
| 非法输入 | 空 prompt、超限、非法 UTF-8、自报身份字段、任意工作区路径、无共享授权、失效路径、隔离目录符号链接均拒绝 | PASS |
| 队列与取消 | 共享工作区等待已有锁；正常取消使其不启动；运行中取消传递到现有运行状态；dequeue 按核实用户而非 bot author 判断归属 | PASS |
| 错误与超时 | 创建/发布失败、Discord 超时、queue 拒绝、runner 启动失败、runner 超时保留真实失败状态与错误证据 | PASS |
| 存储失败 | 入队和启动前 checkpoint 写失败均不执行 runner；损坏 DB 不被清空重建 | PASS |
| 重启恢复 | 从磁盘重载 queued 请求后用同一队列恢复，不再建 thread；creating/publishing/starting/started 不自动重跑 | PASS |
| 本机连接 | 启动实际 Unix socket，运行实际 CLI 子进程提交/查询/冲突；核对目录 0700、socket 0600；拒绝不安全目录及覆盖活动 socket | PASS |
| CLI 不确定状态 | 离线连接与真实慢 HTTP socket 超时返回非零和 unknown，不宣称提交成功 | PASS |
| 隐式 POST 重试 | 实际 discord.js REST 实现配 502 传输替身，断言只调用一次；普通 client 的 retries 仍为 3；真实 MessagePayload 附件及 mentions 编码正确 | PASS |
| 模型真实性 | 未观测时 null；只报告匹配 session 的观测模型，不拿配置默认值或之后另一 session 的模型冒充 | PASS |

## 证据边界

集成测试中的 Discord 对象及 Codex 输出是替身，没有登录生产 Discord，没有启动真实 Codex 任务。进程重启恢复用磁盘重载和重建提交服务模拟，覆盖恢复分支，不等于已执行生产 launchd 重启。

实际验收还需授权：整合代码、配置 `CODEX__LOCAL_TASK_SOCKET_DIR`、等待空闲后重启 Codex bot，再验证提交、真实 thread、Codex 实际启动、结果送回及用户后续消息继续同一 session。操作步骤和 CLI 示例见 [本机任务提交.md](本机任务提交.md)。
